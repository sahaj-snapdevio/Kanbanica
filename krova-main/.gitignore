# dependencies
graphify-out
/node_modules
/.pnp
.pnp.*
.yarn/*
!.yarn/patches
!.yarn/plugins
!.yarn/releases
!.yarn/versions

# testing
/coverage

# next.js
/.next/
/out/

# production
/build

# misc
.DS_Store
*.pem

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

# env files (ignore all real env files; the *.example templates ARE committed)
.env*
!.env.example
!.env.test.example
# .env.unit holds ONLY dummy placeholders for the pure unit suite (no secrets) —
# committed so `pnpm test` works on a fresh clone / in CI without setup.
!.env.unit
!.env.e2e.example

# embedded postgres (dev)
/.pgdata

# local E2E run logs / scratch (host lifecycle runs — never committed)
.e2e-logs/

# typescript
*.tsbuildinfo
next-env.d.ts
